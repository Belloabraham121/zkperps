# Server
PORT=4000
NODE_ENV=development

# JWT (generate a random secret, e.g. openssl rand -hex 32)
JWT_SECRET=your-jwt-secret-at-least-32-chars

# Privy (from https://dashboard.privy.io)
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# App signer: backend signs tx on behalf of user (no popup).
# Create key: openssl ecparam -name prime256v1 -genkey -noout -out private.pem
# Register public key in Privy Dashboard → Authorization keys → New key quorum.
#
# Local: use path to PEM file.
AUTHORIZATION_PRIVATE_KEY_PATH=./private.pem
#
# Vercel/serverless: paste PEM content as env var (no file upload). Use either:
#   - Multi-line: paste the full PEM including -----BEGIN/END----- (Vercel supports multiline).
#   - Single-line: replace real newlines with \n, e.g. -----BEGIN EC PRIVATE KEY-----\nMHQ...\n-----END EC PRIVATE KEY-----
# AUTHORIZATION_PRIVATE_KEY="-----BEGIN EC PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END EC PRIVATE KEY-----"
PRIVY_KEY_QUORUM_ID=

# Chain (Arbitrum Sepolia)
CHAIN_ID=421614
RPC_URL=https://arb-sepolia.g.alchemy.com/v2/YOUR_KEY

# Contracts (Arbitrum Sepolia — match PERPS_IMPLEMENTATION_PLAN.md)
PRIV_BATCH_HOOK=0xe3ea87fb759c3206a9595048732eb6a6000700c4
PERP_POSITION_MANAGER=0xf3c9cdbaf6dc303fe302fbf81465de0a057ccf5e
MOCK_USDC=0x3cbe896e5e4093d6bf8dc0dc7a44c50552c0651e
MOCK_USDT=0x3c604069c87256bbab9cc3ff678410275b156755
MARKET_ID=0x0000000000000000000000000000000000000001

# Base is currency0 in pool (USDT=0, USDC=1)
BASE_IS_CURRENCY0=true

# Optional: perp batch keeper (interval-based auto-execute when ready).
# Batch also auto-executes after each reveal when 2+ pending and interval passed (no keeper needed).
# KEEPER_PRIVY_USER_ID=did:privy:...
# KEEPER_INTERVAL_MS=60000
# KEEPER_MAX_PERP_BATCH_SIZE=0
